import Navigation from "./Navigation.js";

function Header({handleSubmit}){

    return(
        <header>
            <h1>Header - Vídeos</h1>
            <p>Form</p>
            <Navigation/>
        </header>
        
    );
}

export default Header;